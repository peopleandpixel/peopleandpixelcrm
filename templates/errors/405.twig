{% extends 'layout.twig' %}
{% block content %}
<section class="space-y-3">
  <div class="alert alert-warning">
    <span>405 – {{ __('Method Not Allowed')|e }}</span>
  </div>
  <p class="opacity-80">{{ __('The HTTP method used is not allowed for this URL.')|e }}</p>
  <p><a class="btn" href="{{ url('/') }}">&larr; {{ __('Back to home')|e }}</a></p>
</section>
{% endblock %}